<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxxx.mapper.UserMapper">
    <select id="queryUserByName" parameterType="String" resultType="com.xxxx.entity.User">
        select * from tb_user where userName = #{userName}
    </select>
<!--    <sql id="updateid" >truncate table tb_user</sql>-->
    <insert id="saveUser" parameterType="com.xxxx.entity.User" >
        insert into tb_user (userName,userPwd,Email) values(#{userName},#{userPwd},#{Email});
    </insert>
</mapper>